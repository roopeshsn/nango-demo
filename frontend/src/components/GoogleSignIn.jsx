import React from 'react'

function GoogleSignIn() {
  const handleClick = (e) => {
  }

  return (
    <span>
        <button onClick={handleClick} className="px-4 py-2 bg-black text-white rounded text-sm">Google Sign in</button>
    </span>
  )
}

export default GoogleSignIn